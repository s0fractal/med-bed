// axios.essence.js - Distilled to primordial form
// Purity: 1.00

// fetch essence (100%)
function fetch(url, options = {}) {
  // Pure HTTP essence from request (2011)
  return require('https').get(url, options);
}

module.exports = {
  fetch,
};