// express.essence.js - Distilled to primordial form
// Purity: 1.00

// serve essence (100%)
function serve(port, handler) {
  // Pure server essence from express (2010)
  return require('http').createServer(handler).listen(port);
}

module.exports = {
  serve,
};